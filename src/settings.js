const settings = {
    palette: ["#f00"],
    numSliders: 45,
    sliderHeightMin: 0.1,
    sliderHeightMax: 1.06,
    slowestSliderPeriodMs: 2000,
    fastestSliderPeriodMs: 1000,
    screenMargin: 0.05,
    sliderWidthMin: 0.01,
    sliderWidthMax: 0.25,
};

export default settings;